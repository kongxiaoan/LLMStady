# Java 六大设计原则

Java 常见的六大设计原则在中文语境里通常包括：

- 单一职责原则
- 开闭原则
- 里氏替换原则
- 接口隔离原则
- 依赖倒置原则
- 迪米特法则

在 Android 项目里，最常见的问题通常是：

- Activity / Fragment 职责过多，违反单一职责
- 上层直接依赖具体实现，违反依赖倒置
- 接口设计过大，调用方被迫依赖不需要的方法，违反接口隔离

设计原则的价值不在于背定义，而在于帮助你识别耦合、扩展和维护成本的问题。
